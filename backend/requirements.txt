# Core API
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.10.0
pydantic-settings>=2.1.0
python-multipart>=0.0.6

# Database & Storage
supabase==2.27.1
pinecone-client==6.0.0

# PDF Processing (install separately if needed)
# PyMuPDF==1.23.8
# pdfplumber==0.10.3

# Embeddings & ML (install separately when implementing Phase 4)
# sentence-transformers==2.2.2
# InstructorEmbedding==1.0.1
# torch==2.1.2

# LLM (install separately when implementing Phase 5)
# transformers==4.36.2
# accelerate==0.25.0

# Utilities
python-dotenv==1.0.0
httpx>=0.24,<0.26
